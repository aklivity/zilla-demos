name: Check Vortex UI Build

on:
  pull_request:
    branches:
      - main
    paths:
      - 'vortex/ui/**/*'

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Build
        run: docker build vortex/ui/.
